import { SimpleSyncValidatorDirective } from './simple-sync-validator.directive';

describe('SimpleSyncValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleSyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
