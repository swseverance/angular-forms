import { ComplexSyncValidatorDirective } from './complex-sync-validator.directive';

describe('ComplexSyncValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ComplexSyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
